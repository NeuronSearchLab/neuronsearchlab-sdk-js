---
"@neuronsearchlab/sdk": minor
---

Add production-grade server authentication with async token providers, OAuth client-credentials acquisition, in-memory expiry caching, singleflight refreshes, and one refresh-and-retry after a 401 while retaining static access-token compatibility. Add caller-owned event deduplication keys and normalize supported aliases to `deduplication_id` for retry-safe ingestion.
